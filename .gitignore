# secrets
.env
.env.production
.env.local
*.pem
*.key
*_ed25519*
!*_ed25519*.pub.example

# operator-owned state
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/terraform.tfvars

# runtime
caddy_data/
caddy_config/
mediamtx_data/
auth-shim-logs/

# go
auth-shim/auth-shim
auth-shim/*.exe

# MediaMTX TLS material (operator-placed; never commit private keys or certs)
mediamtx/certs/*.crt
mediamtx/certs/*.key
mediamtx/certs/*.pem

# IDE
.vscode/
.idea/
